# Task 1: single stage. Works, but the SDK, the source and obj/ all end up in the image.
FROM mcr.microsoft.com/dotnet/sdk:10.0
WORKDIR /src
COPY . .
RUN dotnet publish -c Release -o /app/publish
CMD ["dotnet", "/app/publish/HelloContainer.dll"]
